msg : Hello world - this is from config server for QA
branch: Master
server:
  contextPath: /service-1
  port: 8081
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false
